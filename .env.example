# ─── Spotify OAuth Credentials ───────────────────────────────────────────────
# Obtain these from https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# The redirect URI registered in your Spotify app settings.
# For local development: http://localhost:3000/callback
# For production: https://your-deployed-domain.com/callback
SPOTIFY_REDIRECT_URI=http://localhost:3000/callback

# ─── Server ───────────────────────────────────────────────────────────────────
# Optional: port to listen on (defaults to 3000).
# Heroku, Koyeb, and Railway set this automatically.
PORT=3000
